spring:
  application:
    name: spring-ai-agent-orchestrator

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  agent:
    max-steps: 5
    risky-tools:
      - crm.update_status
      - notification.send_email
